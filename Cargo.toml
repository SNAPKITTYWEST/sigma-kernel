[package]
name = "sigma-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]

[[bin]]
name = "sigma_kernel"
path = "src/main.rs"

[lib]
name = "sigma_kernel"
path = "src/lib.rs"
